/** Historical fixture behavior; these controls are not current product settings. */
export const legacyContextDefaults = {
  ranking: "priority" as const,
  snippetCap: true,
  maxSnippetLines: 40,
  includeSnippets: false,
};
